config1:
  # Baseline configuration used by main.py. Dataset-specific best-known
  # overrides are defined in run_best_experiments.py and exposed through CLI.
  model_name: monet
  emd_dim: 32
  gfno_hidden: 32
  energy_splits: [0.7, 0.95]
  topk_edges: 3
  ecc_layers: 1
