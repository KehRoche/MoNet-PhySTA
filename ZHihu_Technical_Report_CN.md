# 从神经算子到图时空预测：PhySTA / MoNet-Phy 的方法构造思路

> 这是一份面向知乎、公众号等平台的技术说明稿。重点不是复述实验表格，而是解释我们为什么从神经算子和 FNO 的思想出发，构造一个面向图结构时空系统的物理启发式模型。

## 1. 一个更大的背景：从函数拟合到算子学习

传统深度学习通常学习的是一个函数映射：给定一个输入向量或张量，输出一个类别、数值或另一组张量。这个视角在图像分类、文本建模和常规时间序列预测中非常自然。

但许多科学计算和时空预测问题并不只是“输入一个样本，输出一个标签”。它们更像是在学习一个系统的演化规律：给定某个初始场、边界条件、外部驱动或历史观测，预测未来一段时间的状态。这里真正需要学习的对象不是普通函数，而是从一个函数空间到另一个函数空间的映射，也就是“算子”。

可以直观地理解：

- 普通神经网络学习的是 `x -> y`；
- 神经算子学习的是 `u(t, x) -> v(t, x)` 这类场到场的映射；
- 对时空预测而言，我们关心的是“历史观测场如何演化为未来观测场”。

这种视角很重要。交通流量、空气质量、气象变量、流体状态都不是孤立点值，而是空间中一组状态随时间共同变化。把这些问题放在神经算子的框架下，可以让模型更自然地表达“状态场的演化规律”，而不仅是对历史窗口做黑盒回归。

## 2. FNO 的核心思想：在频域里学习全局演化

Fourier Neural Operator，通常简称 FNO，是神经算子中非常有代表性的思想。它的出发点是：很多物理系统的演化可以在频域中更简洁地描述。

在图像或规则网格上，我们可以对输入场做傅里叶变换，把空间信号分解成不同频率的模式。低频通常表示平滑、全局、长程的变化；高频通常表示局部、快速、细节性的变化。FNO 在频域中对这些模式进行变换，再回到原始空间，从而学习一个场到场的映射。

用更直白的话说，FNO 不只是看每个网格点附近发生了什么，而是先把整个场拆成一组全局振动模式，再学习这些模式如何变化。这让它在处理 PDE、流体、气象等连续场问题时很有吸引力。

FNO 带来的启发主要有三点。

第一，时空预测不一定要完全依赖局部卷积或逐点注意力。频域表示可以更直接地捕捉全局结构。

第二，模型可以把复杂动态分解为不同尺度的模式。低频负责全局趋势，高频负责局部扰动，这比把所有变化混在原空间里更清晰。

第三，算子学习关心的是映射规律本身，而不是某个固定分辨率下的单个输入输出样本。这种思路对于跨场景、跨区域的时空建模很有启发性。

## 3. 但真实交通和空气质量不是规则网格

FNO 的经典形式天然适合规则欧氏空间，例如二维网格、三维场或规则采样的物理变量。但交通和空气质量数据常常不是这种结构。

交通网络由传感器和道路组成。空气质量监测网络由站点和地理关系组成。它们更接近图结构：每个节点有时间序列，每条边表示距离、连通性、相互影响或传播路径。

这就带来一个关键问题：如果 FNO 的傅里叶变换依赖规则网格，那么在图上应该如何定义“频率”？

图信号处理提供了一个自然答案。对于图结构，可以通过图拉普拉斯矩阵定义图上的谱基。直观地说，图上的低频模式表示在相邻节点之间变化较平滑的信号，高频模式表示在相邻节点之间变化剧烈的信号。这样，交通网络或空气质量站点网络也可以拥有自己的“频域”。

这也是 MoNet-Phy 的方法构造起点：把 FNO 的频域算子思想推广到图结构时空系统中，让模型在图谱域和时间维度上共同学习动态演化。

## 4. PhySTA / MoNet-Phy 想解决的问题

我们的核心问题可以概括为：如何在图结构上学习一个兼具物理启发和数据适应性的时空演化算子？

这个问题包含三层含义。

第一，模型需要处理时间演化。未来状态不是独立生成的，而是由历史状态和外部条件驱动。

第二，模型需要处理图上的空间传播。一个节点的状态会受邻近节点、远距离相关节点以及整体网络结构影响。

第三，模型需要处理不同数据集中的条件变量。交通数据主要包含流量与时间标识，而空气质量数据包含更多协变量。如果忽略这些输入语义，模型可能在形式上能跑通，但实际建模对象已经偏离。

因此，MoNet-Phy 没有把所有信息简单塞进一个统一黑盒模块，而是把时空系统拆成几个互补部分：动态场建模、空间扩散建模、时间与协变量编码，以及最终的融合预测。

## 5. 方法构造一：图谱域动态场

MoNet-Phy 中的动态场模块继承了 FNO 的核心直觉：不要只在原始节点空间中做局部混合，而要把图上的信号分解为谱域模式，再学习不同模式的演化。

在规则网格中，傅里叶基来自欧氏空间；在图结构中，谱基来自图拉普拉斯。图拉普拉斯编码了节点之间的连接关系，因此它定义出的谱模式天然携带图结构信息。

低频图谱模式可以理解为网络上的整体趋势，例如一片区域整体拥堵或整体污染浓度升高。高频图谱模式可以理解为局部差异，例如某些站点突然出现异常峰值。通过能量分段和谱域建模，模型可以区分不同尺度的图时空变化。

这种构造带来的启发是：时空预测中的“空间相关性”不一定只靠消息传递或注意力来学，也可以通过图谱域的方式显式组织。谱域表示让模型更接近物理系统中的模态分解思想。

## 6. 方法构造二：空间扩散与局部传播

图谱域动态场擅长表达全局和多尺度模式，但真实图系统中还有非常具体的局部传播机制。

例如交通拥堵可能沿道路方向传播，空气污染可能受邻近站点、风场和地理分布影响。仅依赖全局谱模式，可能不足以捕捉这些局部传播细节。

因此，MoNet-Phy 同时引入空间扩散模块，用图边和 top-k 结构建模节点之间的局部影响。可以把它理解为一种补充机制：谱域动态场负责“系统状态整体怎样演化”，空间扩散模块负责“节点之间怎样相互传递影响”。

这两个分支不是互相替代，而是互补。前者提供全局动态视角，后者提供图上传播视角。

## 7. 方法构造三：时间与协变量编码

时空系统的演化通常不是自治的。交通流量和空气质量都强烈依赖外部条件。

交通数据中，早高峰和深夜的动力学不同；工作日和周末的模式不同。空气质量数据中，除时间因素外，还可能受气象、日期、月份等协变量影响。

因此，MoNet-Phy 在输入端显式编码时间和协变量。对交通数据，模型使用目标值、一天中的时间和一周中的日期等信息；对空气质量数据，模型保留额外协变量路径，并通过侧分支进行编码。

这也是代码里严格固定 `input_dim` 的原因：

| Dataset | input_dim | 含义 |
|---|---:|---|
| PEMS-BAY | 3 | 目标值 + 时间特征 |
| SD | 3 | 目标值 + 时间特征 |
| KnowAir | 15 | 目标值 + 时间特征 + 空气质量协变量 |
| BJAir | 18 | 目标值 + 时间特征 + 空气质量协变量 |

这个设计看似工程化，但背后是方法论问题：模型必须知道输入通道的语义。如果把空气质量协变量误裁掉，或者把交通数据当成空气质量数据处理，预测问题就已经变了。

## 8. 方法构造四：多分支融合预测

最终预测不是由单一模块直接输出，而是由多个互补信息源融合得到。

可以把 MoNet-Phy 的预测路径理解为：

```text
历史观测与协变量
      |
      |-- 时间/条件编码
      |-- 图谱域动态场建模
      |-- 空间扩散建模
      |
   多分支特征融合
      |
   未来多步预测
```

这种设计的关键不在于堆叠更多模块，而在于给不同类型的信息分配合适的位置。

时间编码回答“当前处于什么时间和条件”；图谱域动态场回答“系统整体状态如何演化”；空间扩散模块回答“节点之间如何传播影响”；融合层则把这些信息合并成预测。

## 9. 与普通时空 GNN 的区别

普通时空 GNN 往往以消息传递为核心。节点从邻居接收信息，再通过循环网络、卷积、注意力或 MLP 进行时间建模。这类方法很有效，但它们通常把图结构当成局部信息聚合模板。

MoNet-Phy 更强调“图上的时空算子”这一视角。它不只问邻居给当前节点传来什么信息，还问整个图信号可以分解成哪些动态模式，以及这些模式如何随时间演化。

换句话说，普通时空 GNN 更像是在图上做深度特征传播；MoNet-Phy 更像是在学习一个图结构系统的演化算子。这个差别是本文想强调的主要启发。

## 10. 实验在这里扮演什么角色

这篇介绍不打算用大量篇幅讨论实验表格，因为实验只是用来回答一个问题：这样的构造是否能在真实数据上跑通，并达到接近论文主结果的水平？

当前整理后的开源版本在 KnowAir、PEMS-BAY 和 SD 上与论文 Table 1 的 `Mask=0` 结果接近。其中 KnowAir 略优于论文数值，PEMS-BAY 补跑后通过 best-validation reload 校验，SD 的 RMSE 略优但 MAE/MAPE 略差。整体上，这些结果支持一个较温和的结论：该方法构造在真实图时空预测任务中是有效且可复现的。

更详细的数值表、命令行和复现说明可以放在开源仓库中，而不是占用这篇技术报告的主体篇幅。

## 11. 这项工作给我们的启发

PhySTA / MoNet-Phy 的启发不只是一个具体模型，而是一种建模路线。

第一，当问题对象是时空场时，可以从“学习样本映射”转向“学习系统算子”。这个视角能帮助我们更自然地表达动态演化。

第二，当空间结构是图而不是规则网格时，可以把 FNO 的频域思想迁移到图谱域。图拉普拉斯提供了图上的频率概念，使得全局模式和局部扰动可以被区分。

第三，物理启发不等于手写物理方程。很多真实系统的机理复杂且不完全可知，数据驱动模型仍然必要。更实际的做法是把物理系统的结构性先验放进模型组织方式中，例如动态场、扩散、谱分解和条件驱动。

第四，可复现性本身也是方法的一部分。对于复杂时空模型，如果没有清晰的数据检查、配置说明、结果汇总和发布校验，模型思想很难真正被社区复用。

## 12. 适用范围与局限

MoNet-Phy 更适合有明确图结构、节点时间序列和空间传播关系的问题，例如交通网络、空气质量监测网络和传感器网络。

如果数据没有稳定图结构，或者节点关系主要由未观测因素决定，那么图谱域建模未必带来收益。如果协变量语义不清晰，空气质量这类多通道输入也可能引入噪声。

此外，谱域方法依赖邻接矩阵质量。图结构如果构造得不合理，频域分解也会随之偏离真实传播规律。因此，在迁移到新任务时，图构造本身应当被认真对待。

## 13. 面向读者的总结

如果用一句话总结这项工作：PhySTA / MoNet-Phy 尝试把神经算子、FNO、图谱域建模和时空扩散统一起来，用于真实图结构时空预测。

它不是把 FNO 原封不动搬到交通或空气质量数据上，而是围绕图结构做了重新组织：用图拉普拉斯定义频率，用动态场学习全局演化，用空间扩散补充局部传播，用时间与协变量编码描述外部驱动。

这种方法构造的意义在于，它提供了一条从“黑盒预测模型”走向“结构化时空算子模型”的路径。对图神经网络、神经算子、物理启发机器学习和时空预测感兴趣的读者，可以把它作为一个可继续拓展的起点。

## 附：适合知乎的标题备选

1. **从 FNO 到图时空预测：我们如何构造一个物理启发的神经算子模型**
2. **神经算子不只属于 PDE：它也能启发交通与空气质量预测**
3. **把 FNO 搬到图上：PhySTA / MoNet-Phy 的方法构造思路**
4. **不只是时空 GNN：从图谱域动态场理解 MoNet-Phy**

## 附：文末开源说明

我们已经整理了 MoNet-Phy 的开源复现仓库，包含训练入口、数据检查、推荐参数、结果汇总和 release validation。开源版本重点支持 `Mask=0` 设置下的 PEMS-BAY、SD、KnowAir 和 BJAir 实验。

如果你想复现，建议先运行：

```bash
python scripts/check_data.py --datasets PEMS-BAY SD KnowAir BJAir
python scripts/validate_release.py
```

再根据仓库中的 `REPRODUCIBILITY.md` 启动长实验。实验指标存在小幅波动是正常现象，建议同时报告命令、commit、硬件环境和日志汇总结果。
