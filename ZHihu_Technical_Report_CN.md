# 从物理启发的时空建模到可复现实验：PhySTA / MoNet-Phy 技术解读

> 这是一份面向知乎、公众号等平台的说明性技术报告草稿。它不替代论文正文，也不刻意复述公式推导，而是用更易读的方式解释：我们为什么做 PhySTA / MoNet-Phy、方法解决什么问题、实验结果说明了什么，以及开源代码如何复现。

## 一句话概括

PhySTA / MoNet-Phy 是一个面向图结构时空预测的物理启发式学习框架。它把时间序列预测中的动态演化、图上的空间传播、时间与协变量编码放在一个统一模型中，用于交通流量和空气质量等真实图时序场景。

在目前整理后的开源版本中，我们重点提供 `Mask=0` 预测设置下的可复现实验代码，并给出 PEMS-BAY、SD、KnowAir 和 BJAir 四个数据集上的运行配置、数据检查脚本、结果汇总脚本和发布校验脚本。

## 为什么图时空预测仍然难

很多现实世界数据天然不是一条独立时间序列，而是带有空间结构的时空系统。

交通网络中，一个传感器点位的拥堵会沿道路传播；空气质量中，一个监测站的污染浓度既受本地气象影响，也受周边区域传输影响。这类问题至少有三个难点：

1. **时间演化不是孤立的。** 当前状态不仅依赖过去的观测值，还依赖日周期、周周期、月份、天气等外部条件。
2. **空间关系不是简单网格。** 传感器或站点构成的是图，不是规则图像；节点之间的影响可能有方向、强弱和多尺度差异。
3. **模型效果和复现稳定性都很重要。** 一个模型如果只能在内部环境里跑出结果，很难支撑后续研究复用。

PhySTA / MoNet-Phy 的出发点是：与其只把问题看作“把历史序列喂给黑盒网络”，不如显式建模图上的时空演化，把动态场、空间扩散和条件编码拆开处理，再进行融合预测。

## 方法直觉：把时空系统拆成三个互补部分

当前实现中的 MoNet-Phy 可以理解为三个核心分支的组合。

### 1. 时间与条件编码：让模型知道“什么时候、在什么条件下”

模型首先对输入序列做时空标识编码。对于交通数据，输入主要包含目标值、一天中的时间位置和一周中的日期位置；对于空气质量数据，输入维度更高，还包含额外协变量。

这也是为什么代码中对 `input_dim` 有严格要求：

| Dataset | Required input dim |
|---|---:|
| PEMS-BAY | 3 |
| SD | 3 |
| KnowAir | 15 |
| BJAir | 18 |

这种设计避免了一个常见问题：如果把交通数据和空气质量数据都当成相同格式输入，模型可能在维度上能跑通，但语义已经错了。开源代码中，`experiments/monet/main.py` 会根据数据集名自动覆盖 `input_dim`，从源头上减少错误配置。

### 2. 物理启发的动态场：让模型学习图上的时空演化

MoNet-Phy 使用图傅里叶/谱域思想来处理图上的动态演化。直观地说，图上的变化可以分解为不同“频率”的模式：低频对应更平滑、更全局的变化，高频对应更局部、更剧烈的变化。

在实现中，模型会基于邻接矩阵构造拉普拉斯相关表示，并将动态场建模交给 `GTFNO2d` 等模块。这样做的动机是让模型不只记忆局部历史模式，也能利用图结构中隐含的传播规律。

工程上还有一个细节：早期 CUDA/PyTorch 组合在复杂数计算上可能触发 kernel 兼容问题。因此当前开源版本将 directed Laplacian 的初始化放到 CPU 上完成，再把结果交给模型使用。这不是论文主张的一部分，但对开源复现非常关键。

### 3. 空间扩散模块：补充节点之间的图传播关系

除了动态场分支，模型还包含空间扩散/图传播模块 `MSKGN`。它利用邻接矩阵和 top-k 图边信息建模节点之间的传播关系。

可以把两个分支的关系理解为：

- 动态场分支关注“图上的时空状态如何演化”；
- 空间扩散分支关注“节点之间如何相互影响”；
- 最后的融合层把目标值、物理场输出、空间传播特征以及协变量侧信息整合起来，生成预测结果。

这种拆分让模型结构更贴近时空系统本身，而不是把所有信息都压进一个单一黑盒编码器。

## 实验设置：以 Mask=0 主预测场景为核心

当前开源版本重点整理了 `Mask=0` 设置下的主实验复现。使用的数据集包括：

- **PEMS-BAY**：交通传感器数据；
- **SD**：交通网络数据；
- **KnowAir**：空气质量数据；
- **BJAir**：额外空气质量实验数据。

论文 Table 1 中直接对比的是 KnowAir、PEMS-BAY 和 SD。BJAir 当前作为额外实验保留，不作为 Table 1 复现结论的一部分。

当前推荐配置如下：

| Dataset | emd_dim | gfno_hidden | energy_splits | topk_edges | ecc_layers | batch size | seed |
|---|---:|---:|---|---:|---:|---:|---:|
| PEMS-BAY | 32 | 32 | [0.7, 0.95] | 3 | 1 | 32 | 2023 |
| SD | 32 | 32 | [0.7, 0.95] | 3 | 1 | 32 | 2023 |
| KnowAir | 16 | 8 | [0.7, 0.95] | 3 | 1 | 32 | 2023 |
| BJAir | 16 | 8 | [0.7, 0.95] | 3 | 1 | 32 | 2023 |

共同训练设置为 `seq_len=12`、`horizon=12`、`max_epochs=80`、`patience=15`、`learning_rate=0.002`、`mask_ratio=0.0`。

## 当前复现结果

基于当前本地日志汇总，结果如下：

| Dataset | Current MAE | Paper MAE | MAE Delta | Current RMSE | Paper RMSE | Current MAPE | Paper MAPE |
|---|---:|---:|---:|---:|---:|---:|---:|
| KnowAir | 20.3364 | 20.5500 | -0.2136 | 32.8287 | 33.0500 | 0.5358 | 0.5500 |
| PEMS-BAY | 1.6863 | 1.6600 | +0.0263 | 3.7290 | 3.6100 | 0.0389 | 0.0400 |
| SD | 21.2282 | 20.6400 | +0.5882 | 32.6871 | 33.0500 | 0.1592 | 0.1500 |
| BJAir | 49.9713 | - | - | 73.1515 | - | 1.7079 | - |

这些结果说明：

- KnowAir 与论文结果非常接近，并在 MAE、RMSE、MAPE 上略优于论文 Table 1 的 Mask=0 数值。
- PEMS-BAY 补跑后已通过 strict provenance 校验，日志中确认测试前加载了 best validation model。当前 MAE/RMSE 略高于论文，但 MAPE 略优，整体差距较小。
- SD 的 RMSE 略优于论文，MAE/MAPE 略差。这个差异在实际复现实验中可以接受，但应如实报告。
- BJAir 没有论文 Table 1 对应项，因此更适合作为额外实验展示，而不是论文主表复现声明。

这里需要强调一点：复现实验不应该追求“逐小数点完全一致”。硬件、CUDA/PyTorch 版本、随机种子、早停点和数据预处理细节都会带来小幅波动。更重要的是，代码路径、数据设置、日志和汇总方式要透明。

## 开源复现：我们做了哪些工程整理

这次开源整理不只是把代码放出来，还补齐了复现所需的辅助脚本和说明文件。

### 数据检查

运行长实验前可以先检查数据文件、维度、节点数、索引范围和邻接矩阵：

```bash
python scripts/check_data.py --datasets PEMS-BAY SD KnowAir BJAir
```

KnowAir 和 BJAir 当前本地索引中有少量边界样本会被 DataLoader 过滤。脚本默认将其报告为 warning，而不是直接失败。这样可以避免外部用户把正常边界过滤误判为数据损坏。

### 一键复现实验配置

可以使用统一 runner 启动主实验：

```bash
python experiments/monet/run_best_experiments.py --datasets PEMS-BAY SD KnowAir --device cuda:0
```

也可以只跑单个数据集，例如 PEMS-BAY：

```bash
python experiments/monet/run_best_experiments.py --datasets PEMS-BAY --device cuda:0
```

runner 会把结果写入：

```text
eval/monet/best_runs/results.csv
eval/monet/best_runs/results.json
eval/monet/best_runs/<DATASET>_stdout.log
```

### 结果汇总与发布校验

完成实验后，可以用：

```bash
python scripts/summarize_results.py --no_write
```

生成当前日志对应的结果表。

最终发布前，可以运行：

```bash
python scripts/validate_release.py
python scripts/validate_release.py --check_data
python scripts/validate_release.py --strict_results
```

其中 `--strict_results` 不要求数值和论文完全一致，而是检查最新完成的论文数据集实验是否经过当前 best-validation reload 流程。这一点对于避免“拿旧日志做新代码声明”很有用。

## 方法的适用边界

PhySTA / MoNet-Phy 更适合有明确图结构、时间演化和空间传播关系的数据场景，例如交通网络、空气质量站点网络和传感器网络。

它不一定适合所有时间序列问题。如果数据没有稳定图结构，或者节点之间的关系主要由非图因素决定，那么直接引入图谱域建模未必带来收益。空气质量数据还依赖较多协变量，因此输入通道的语义必须严格保持一致。

从开源角度看，我们也不把当前结果包装成“绝对 SOTA 一次运行复现”。更严谨的说法是：在当前整理后的代码、配置和数据条件下，主要指标与论文 Table 1 Mask=0 结果接近，且复现流程已经脚本化。

## 为什么这项工作值得关注

我认为 PhySTA / MoNet-Phy 的价值主要体现在三个方面。

第一，它尝试把物理启发的动态演化思想带入图时空预测，而不是完全依赖黑盒序列编码。

第二，它把交通和空气质量这两类输入语义差异很大的任务放到同一套框架中处理，同时保留数据集特定的协变量路径。

第三，当前开源版本强调可复现性：从数据检查、运行命令、结果汇总到 release validation，都尽量减少“代码能跑但不知道跑的是什么”的问题。

如果你关心图神经网络、时空预测、交通流量建模、空气质量预测，或者更广义的物理启发机器学习，这个项目可以作为一个可读、可跑、可改的起点。

## 附：推荐的知乎标题

可以根据发布风格选择不同标题：

1. **我们如何用物理启发的图时空模型预测交通与空气质量？**
2. **从图傅里叶到空气质量预测：PhySTA / MoNet-Phy 技术解读**
3. **不只是黑盒预测：一个物理启发的图时空学习框架**
4. **ICLR 2026 投稿工作 PhySTA：面向图结构时空预测的可复现实现**

## 附：适合放在文末的开源说明

我们已经整理了 MoNet-Phy 的开源复现仓库，包含：

- 训练入口与推荐参数；
- 数据布局和数据完整性检查；
- 一键补跑实验脚本；
- 结果汇总脚本；
- release validation；
- 中文/英文复现说明、数据说明和故障排查文档。

如果你准备复现实验，建议先运行数据检查和 dry-run，再启动长实验。指标存在小幅波动是正常现象，建议报告完整命令、commit、硬件环境和日志汇总结果。
